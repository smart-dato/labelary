# The ZPL fixtures are sent verbatim as request bodies, so their bytes have to
# match the recorded HTTP fixtures on every platform. Without this, Git hands
# Windows a CRLF copy, the request no longer matches its cassette, and the
# replayed tests fail there only.
*.zpl text eol=lf
